<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gray-900 text-white flex items-center justify-center min-h-screen"
  >
    <div class="w-full max-w-md bg-gray-800 p-8 rounded-lg shadow-lg">
      <h1 class="text-2xl font-bold text-center mb-6"><%=title%></h1>
      <form action="/register" method="POST" class="space-y-4">
        <label for="email" class="block text-sm font-medium">Email</label>
        <input
          type="text"
          name="email"
          placeholder="Enter email"
          class="w-full p-2 mt-1 bg-gray-700 text-white border border-gray-600 rounded"
          required
        />
        <label for="username" class="block text-sm font-medium">Username</label>
        <input
          type="text"
          name="username"
          placeholder="Enter username"
          class="w-full p-2 mt-1 bg-gray-700 text-white border border-gray-600 rounded"
          required
        />
        <div>
          <label for="password" class="text-sm font-medium">Password</label>
        </div>
        <input
          type="password"
          name="password"
          placeholder="Enter password"
          class="w-full p-2 mt-1 bg-gray-700 text-white border border-gray-600 rounded"
          required
        />
        <button
          type="submit"
          class="w-full bg-blue-500 py-2 text-white rounded hover:bg-blue-600"
        >
          Register
        </button>
      </form>
      <div class="mt-6 text-center text-sm">
        <span class="text-white">Already an account?</span>
        <a href="/login" class="text-blue-400 hover:underline ml-2">Login</a>
      </div>
    </div>
  </body>
</html>
